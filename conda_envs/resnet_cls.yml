name: projekt-resnet-cls
channels:
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - numpy
      - matplotlib
      - tqdm
      - timm
      - opencv-python
      - torch
      - torchvision